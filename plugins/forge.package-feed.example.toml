# Agent Forge plugin mount (M2). Copy into forge plugins/ and add bundle entry in forge.plugins.toml.
id = "package-feed"
assembly = "AgentForge.Plugin.PackageFeed.dll"
tier = "zoo"

# ANPM settings live in forge host appsettings (not here). Example fragment:
#   config/forge.anpm.example.json
